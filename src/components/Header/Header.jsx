
function Header() {
    
    // We add in our selector in here
    //const 

    return(
        <header>
            <h1>Prime Pizza</h1>
            <h2>Total: {}</h2>
            {/* We want to have out total info in the {} above */}
        </header>
    )
}

export default Header;